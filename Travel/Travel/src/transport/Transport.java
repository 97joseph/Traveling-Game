package transport;

/**
 * You can do anything with this interface,
 * including changing it to a class if needed, but not:
 * remove it
 * rename it
 * move it to another package
 */
public interface Transport {
}
